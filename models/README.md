# Saved models

Included:

```text
maintenance_strategy_pipeline.joblib
```

Generated after running deadline training:

```text
deadline_quantile_bundle.joblib
```

Model files are binary artifacts. Rebuild them using the scripts whenever the
data, feature engineering or package versions change.
