# Raw SAP data

Place the workbook here with the exact name:

```text
SAP_notintime_.xlsx
```

This directory is excluded by `.gitignore` because the workbook may contain
company-sensitive SAP data. Never push the raw workbook to GitHub.
